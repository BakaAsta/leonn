{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
    {# <twig:Tableau 
        entityType="{{ entityType }}" 
        titre="{{ titre }}" 
        titreBouton="{{ titreBouton }}" 
        actionButton="{{ actionButton }}" 
        listeTh="{{ liste_th }}" 
        listeAttributs="{{ listeAttributs }}" 
        listeObjets="{{ listeObjets }}" 
        urlModif="{{ urlModif }}" 
        urlSupp="{{ urlSupp }}"
    /> #}
    
    <div {{ react_component('tableauTypeProduit', {
        titre: titre,
        titreBouton: titreBouton,
        token:accessToken
    }) }}></div> 
{% endblock %}
